{
  "collectionName": "components_seo_custom_dimension",
  "info": {
    "displayName": "Custom Dimension",
    "description": "Custom dimension for GA4"
  },
  "options": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "description": "Name of the custom dimension"
    },
    "value": {
      "type": "string",
      "required": true,
      "description": "Value of the custom dimension"
    },
    "scope": {
      "type": "enumeration",
      "enum": ["event", "user", "session", "page"],
      "default": "page",
      "required": true
    }
  }
}
