HOST=0.0.0.0
PORT=1337

# Generate these with a secure random string generator
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=CHANGE_ME_WITH_SECURE_RANDOM_STRING
ADMIN_JWT_SECRET=CHANGE_ME_WITH_SECURE_RANDOM_STRING
TRANSFER_TOKEN_SALT=CHANGE_ME_WITH_SECURE_RANDOM_STRING
JWT_SECRET=CHANGE_ME_WITH_SECURE_RANDOM_STRING

# Database
# SQLite (default)
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# PostgreSQL example
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=strapi
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=strapi
# DATABASE_SSL=false

# File upload settings
# Enable CloudImage for image optimization by setting API key
# CLOUDIMAGE_TOKEN=your_cloudimage_token

# Email configuration (optional)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=user@example.com
# SMTP_PASSWORD=your_password
# SMTP_DEFAULT_FROM=noreply@example.com
# SMTP_DEFAULT_REPLY_TO=info@example.com

# Enable Admin panel features
FLAG_NPS=true
FLAG_PROMOTE_EE=false